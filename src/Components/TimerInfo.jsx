import infoIcon from "../Assets/info-icon.svg";
import { infoScreen } from "../Assets/Settings/Settings";

export default function TimerInfo({ language }) {
  return infoScreen(language, infoIcon);
}
